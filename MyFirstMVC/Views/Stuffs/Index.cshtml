@model IEnumerable<MyFirstMVC.Models.Stuff>

@{
    ViewBag.Title = "Index";
}

<h2>Сотрудники</h2>

<p >
    @Html.ActionLink("Добавление сотрудника", "Create", null, new { @style = "color: white; text-decoration: none " })
</p>
<table class="TextSlots">
    <tr>
        <th>
            Фамилия
        </th>
        <th>
            Имя
        </th>
        <th>
            Отчество
        </th>
        <th>
            Пол
        </th>
        <th>
            Телефон
        </th>
        <th>
            Почта
        </th>
        <th>
            Дата регистрации
        </th>
        <th>
            Дата рождения
        </th>
        <th>
            Должность
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr class="AllSlots">
        <td>
            @Html.DisplayFor(modelItem => item.Surname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patronymic)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gender)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegistrationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BirthdayDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Posts.PostName)
        </td>
        <td>
            <table>
                @for (int i = 0; i < 3; i++)
                {
                    <tr>
                        @if (i == 0)
                        {
                            <td>
                                @Html.ActionLink("Редактировать сотрудника", "Edit", new { id = item.StuffID })
                            </td>
                        }
                        else if (i == 1)
                        {
                            <td>
                                @Html.ActionLink("Подробнее о сотруднике", "Details", new { id = item.StuffID })
                            </td>
                        }
                        else
                        {
                            <td>
                                @Html.ActionLink("Удалить сотрудника", "Delete", new { id = item.StuffID })
                            </td>
                        }
                    </tr>
                }
            </table>
        </td>
    </tr>
}

</table>
